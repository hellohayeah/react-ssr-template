import React from 'react'
import s from './{{pascalCase name}}.module.scss';

const {{pascalCase name}}: React.FC = () => {
  return (
    <div className={s.{{snakeCase name}} }>
      {{pascalCase name}}
    </div>
  )
}

export default {{pascalCase name}}